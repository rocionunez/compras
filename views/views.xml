<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="compras.list">
      <field name="name">compras list</field>
      <field name="model">compras.compras</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="compras.action_window">
      <field name="name">compras window</field>
      <field name="res_model">compras.compras</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="compras.action_server">
      <field name="name">compras server</field>
      <field name="model_id" ref="model_compras_compras"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="compras" id="compras.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="compras.menu_1" parent="compras.menu_root"/>
    <menuitem name="Menu 2" id="compras.menu_2" parent="compras.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="compras.menu_1_list" parent="compras.menu_1"
              action="compras.action_window"/>
    <menuitem name="Server to list" id="compras" parent="compras.menu_2"
              action="compras.action_server"/>
    -->
  </data>
</odoo>